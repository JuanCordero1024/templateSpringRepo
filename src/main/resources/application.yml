# application.yml del Microservicio de Usuarios
roles:
  service:
    # 🔑 La URL de destino apunta a la API Gateway + el prefijo de ruta que activó el Predicado
    url: http://localhost:8080/api/role